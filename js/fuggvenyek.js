
  function hider() {
    document.body.innerHTML = "";  /* declarálom a függvényt */
      }
      hirder();   /* Invocálom a függvényt, ezzel indítom. */
    
      function adder(x, y) {
          return x + y;    /*  return hatására van output a függvényemnek. */
      }
      adder(2, 7);
    
      // clean code  -- Nevek
      let n;
      let nrOfProduct;
      let numberOfProduct;
      // ne legyen a név félrevezető
      function multiplier(x, y)
      return (x * y);
    
      // Kerülni a O és I
      // komment egy soros, nem veszi figyelembe
    
      function getUserRole(user, roles, id); {
            if ( user.admin ) {
                retur user.roles;
              }  else {
                    return user.roleObject;
                }
            }
    //
     let idPattern = /m[z-a]n/;
    